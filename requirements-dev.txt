# Dependencias de desarrollo y pruebas del proyecto MIA_Deteccion_HC.
#
# Este archivo declara explícitamente el framework de pruebas (pytest) y sus
# complementos, que hasta la entrega S10 se usaban sin estar declarados.
#
# Instalación (Windows / PowerShell):
#   .\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
# Instalación (Linux / macOS):
#   python -m pip install -r requirements-dev.txt
#
# Incluye las dependencias de ejecución para poder importar api/, s7/ y s10/
# desde las pruebas.
-r requirements.txt

# --- Núcleo de pruebas (obligatorio) ---------------------------------------
# Versiones mínimas alineadas con el entorno verificado en S11:
# pytest 9.1.1, pytest-cov 7.1.0, coverage 7.16.0 sobre Python 3.12.10.
pytest>=8.0
pytest-cov>=7.0
coverage>=7.6

# --- Suites SDET adicionales (opcional) ------------------------------------
# Necesarias solo para las fases de integración, rendimiento y E2E de tests/.
# La suite unitaria (api/, s7/, s10/, s11/) no las requiere.
httpx>=0.27
pytest-asyncio>=0.23
pytest-benchmark>=4.0
locust>=2.24
playwright>=1.42
pytest-playwright>=0.4
