# Copia este archivo a secrets.toml (gitignored) o usa variables de entorno (.env).
# streamlit run demo/app.py lee secrets.toml automáticamente.

OPENAI_API_KEY = "sk-..."
# OPENAI_BASE_URL = "http://localhost:11434/v1"  # Ollama on-premise (CITIMED)

# Autenticación opcional (fase piloto)
# ENABLE_AUTH = true
# AUTH_USERS = { medico = "cambiar_por_hash_o_texto_plano_demo" }
