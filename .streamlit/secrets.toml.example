# Copiar a secrets.toml (gitignored):
#   copy .streamlit\secrets.toml.example .streamlit\secrets.toml   (Windows)
#
# También puedes usar solo .env en la raíz; la API FastAPI y scripts S7 lo leen con python-dotenv.

OPENAI_API_KEY = "sk-your-key-here"

# OPENAI_BASE_URL = "http://localhost:11434/v1"  # Ollama on-premise (CITIMED / PHI)

# ENABLE_AUTH = true
# AUTH_USERS = { medico = "cambiar_por_hash_o_texto_plano_demo" }
